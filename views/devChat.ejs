<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Bootstrap 5 CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <title>Responsive UI with Centered Text</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100vh;
        background-color: #f0f0f0;
      }

      /* Navbar styling */
      .navbar {
        width: 100%;
        position: fixed;
        top: 0;
        left: 0;
        background-color: #f8f9fa;
        z-index: 2; 
      }

      .container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        position: relative;
        margin-top: 100px; 
      }

      /* Circle (card-body) styling */
      .card-body {
        background-color: blueviolet;
        width: 30vw;
        height: 30vw;
        max-width: 300px;
        max-height: 300px;
        border-radius: 50%;
        transition: all 0.3s ease;
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        animation: bubble 3s infinite ease-in-out;
        display: flex;
        justify-content: center;
        align-items: center;
        position: relative;
        color: white; /* Make text inside white */
        font-size: 2rem;
        font-weight: bold;
      }

      @media (max-width: 600px) {
        .card-body {
          width: 50vw;
          height: 50vw;
          max-width: 200px;
          max-height: 200px;
          font-size: 1.5rem; /* Adjust font size for smaller screens */
        }
      }

      /* Centered text inside the circle */
      .circle-text {
        position: absolute;
        color: white;
        text-align: center;
      }

      /* Keyframe animation for bubbling effect */
      @keyframes bubble {
        0%,
        100% {
          transform: scale(1);
        }
        50% {
          transform: scale(1.1);
        }
      }

      .img-container {
        width: 250px; /* Set desired width for the container */
        height: 250px; /* Set desired height for the container */
        display: flex;
        justify-content: center;
        margin-top: 10px;
      }

      .img-container img {
        max-width: 100%; /* Ensure image scales with the container */
        max-height: 100%; /* Ensure image height doesn't exceed the container */
        object-fit: contain; /* Maintain aspect ratio inside the container */
      }
      .menu {
        position: fixed;
        right: 30px; /* Adjust position */
        bottom: 100px; /* Move menu above the toggle button */
        display: flex;
        flex-direction: column; /* Stack items vertically */
        align-items: center;
      }

      .menu li {
        list-style: none;
        margin: 5px 0; /* Space between menu items */
        transition: transform 0.3s ease; /* Smooth transition for rotation */
      }

      .menuToggle {
        position: fixed; /* Keep the toggle button fixed */
        right: 30px; /* Adjust position */
        bottom: 40px; /* Position below the menu items */
        width: 60px;
        height: 60px;
        background-color: blueviolet;
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
        cursor: pointer;
        font-size: 32px;
        transition: .5s;
        z-index: 100;
      }

      .menu a {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 60px;
        height: 60px;
        text-decoration: none;
        font-size: 26px;
        border-radius: 50%;
        transition: 0.5s;
        color: transparent;
        filter: drop-shadow(0 0 2px var(--clr));
      }

      .menu.active a {
        color: #161616;
      }

      .menu.active .menuToggle {
        transform: rotate(45deg);
      }
    </style>
  </head>
  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a
          class="navbar-brand"
          href="#"
          style="color: blueviolet; font-weight: bold"
          >DevChat</a
        >
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="#">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Features</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Pricing</a>
            </li>
            <li class="nav-item dropdown">
              <a
                class="nav-link dropdown-toggle"
                href="#"
                id="navbarDropdown"
                role="button"
                data-bs-toggle="dropdown"
                aria-expanded="false"
              >
                Dropdown
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                <li><a class="dropdown-item" href="#">Action</a></li>
                <li><a class="dropdown-item" href="#">Another action</a></li>
                <li><hr class="dropdown-divider" /></li>
                <li>
                  <a class="dropdown-item" href="#">Something else here</a>
                </li>
              </ul>
            </li>
          </ul>
          <!-- Right Side: Search and Button -->
          <form class="d-flex">
            <input
              class="form-control me-2"
              type="search"
              placeholder="Search"
              aria-label="Search"
            />
            <button class="btn btn-outline-success" type="submit">
              Search
            </button>
          </form>
        </div>
      </div>
    </nav>

    <!-- Main Content -->
    <div class="container">
      <!-- Circular animated div with centered text -->
      <div id="round1" class="card-body">
        <div class="circle-text">DevChat.!</div>
      </div>

      <!-- Image below the circle -->
      <div class="img-container">
        <img
          src="./Screenshot_2024-09-23_142129-removebg-preview.png"
          alt="Image"
        />
      </div>
    </div>

    <ul class="menu">
      <div class="menuToggle">+</div>
      <li>
        <a href="/home"><ion-icon name="home-outline"></ion-icon></a>
      </li>
      <li>
        <a href="#"><ion-icon name="settings-outline"></ion-icon></a>
      </li>
      <li>
        <a href="#"><ion-icon name="logo-wechat"></ion-icon></a>
      </li>
      <li>
        <a href="/video"><ion-icon name="videocam-outline"></ion-icon></a>
      </li>
    </ul>

    <script
      type="module"
      src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"
    ></script>
    <script
      nomodule
      src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"
    ></script>

    <script>
      let menuToggle = document.querySelector(".menuToggle");
      let menu = document.querySelector(".menu");
      menuToggle.onclick = function () {
        menu.classList.toggle("active");
      };
    </script>

    <script>
      document.addEventListener("DOMContentLoaded", function () {
        // Get the card body element
        const cardBody = document.querySelector(".card-body");

        // Add a click event listener to toggle the bubble effect
        cardBody.addEventListener("click", function () {
          cardBody.classList.toggle("bubble");
        });
      });
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
  </body>
</html>